[project]
name = "hello-world-mcp"
version = "0.1.0"
description = "Production-grade MCP server with weather information tools"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.26.0",
    "httpx>=0.25.0",
    "pydantic>=2.5.0",
    "python-dotenv>=1.0.0",
]
